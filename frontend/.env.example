# Optional: base URL of the Go view-counter API (e.g. http://localhost:8080 when running go locally)
# When set, the app will send a view ping on each page load and show "This page: N views".
# PUBLIC_GO_API_URL=http://localhost:8080
